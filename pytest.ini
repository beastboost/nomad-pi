[pytest]
testpaths = tests
# The async runtime-mode guards are plain `async def` tests. Without this they
# are collected, skipped with a warning, and reported as failures — which read
# as two broken guards rather than a missing plugin.
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
