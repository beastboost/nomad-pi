name: Nomad Android APK

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'android/**'
      - '.github/workflows/android-native.yml'

permissions:
  contents: read

jobs:
  build:
    name: Test and build installable native APK
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Enforce native client boundary
        run: |
          if grep -R -n -E 'android\.webkit\.WebView|TrustedWebActivity|androidx\.browser\.trusted' android/app/src/main android/app/build.gradle.kts; then
            echo 'Nomad Android must remain a native client, not a WebView/TWA wrapper.' >&2
            exit 1
          fi

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android platform
        run: sdkmanager 'platforms;android-36' 'build-tools;35.0.0'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.13'

      - name: Compile and run unit tests
        run: gradle -p android :app:testDebugUnitTest :app:assembleDebug --stacktrace

      - name: Upload installable APK
        uses: actions/upload-artifact@v4
        with:
          name: Nomad-Android-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
          retention-days: 30
